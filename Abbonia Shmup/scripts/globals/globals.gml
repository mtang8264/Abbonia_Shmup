/// The minimum position that is within the viewport assuming the sprite is drawn from the top left.
#macro viewport_minimum new vector2(100, 100)
/// The maximum position that is within the viewport assuming the sprite is drawn from the bottom right.
#macro viewport_maximum new vector2(740, 460)